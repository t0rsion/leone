# v0.8 intervention record

The first frozen GPU study failed. Plan availability constructed four dense
vocabulary distributions before every controller decision. The failure receipt
is [`v0.8-correctable-20260824T082035Z.json`](../receipts/v0.8-correctable-20260824T082035Z.json).

The intervention replaced those allocations with history predicates. It did
not change the manifest, prompts, target path, proposal distributions, or
controller gates. The next idle study passed. The release receipt is
[`v0.8-correctable-20260824T082908Z.json`](../receipts/v0.8-correctable-20260824T082908Z.json).

Two later instrumentation studies ran while an unrelated process owned a CUDA
context. Their receipts remain immutable, but they are not release evidence:

- [`v0.8-correctable-20260824T084448Z.json`](../receipts/v0.8-correctable-20260824T084448Z.json)
- [`v0.8-correctable-20260824T084956Z.json`](../receipts/v0.8-correctable-20260824T084956Z.json)

The current harness queries the CUDA compute-process table before and after a
timing study. It rejects any process other than its own. An oracle-only run
does not need an idle GPU.
